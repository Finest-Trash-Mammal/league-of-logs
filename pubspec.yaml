name: summoners_lift
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  english_words: ^4.0.0

  shared_preferences: ^2.1.0

  flutter_native_splash: ^2.0.0

  window_size:
    git:
      url: https://github.com/google/flutter-desktop-embedding
      path: plugins/window_size

  logger: ^1.4.0
  sqflite: ^2.0.0+3
  path_provider: ^2.0.9

  path: ^1.8.0

  provider: ^6.0.0
  sqflite_common_ffi: ^2.3.4+4
  fl_chart: ^0.70.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  test: ^1.22.0

flutter_native_splash:
  color: "#ffffff" 
  image: "assets/images/Braum_10.jpg" 

flutter:
  uses-material-design: true

  generate: true

  assets:
    - assets/images/
